logging:
  level:
    root: INFO
    # Explicit INFO for the module (the DEBUG default is dev-only, set in
    # application-dev.yml). Keeps prod logs quiet + cheaper in Log Analytics.
    za.co.csnx.demo: INFO
spring:
  jpa:
    show-sql: false

app:
  security:
    cors:
      allowed-origins:
        - http://localhost:8082
